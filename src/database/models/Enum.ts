export const USER_ENUM = Object.freeze({
  ROLES: ['ADMIN', 'USER'],
  VERIFY: ['pending', 'verified']
});

export const BOOK_ENUM = Object.freeze({
  STATUS: ['FREE', 'PAID']
});

export const CHAPTER_ENUM = Object.freeze({
  STATUS: ['FREE', 'PAID']
});
